# 快速测试指南 - Win10 卡死修复

## 🎯 核心问题
您的任务管理器显示有 **7-8个进程同时运行**，总CPU占用超过100%，这就是卡死的原因！

## ✅ 已修复内容

### 🔑 最关键修复：单实例锁
```typescript
// electron/main.ts
const gotTheLock = app.requestSingleInstanceLock()

if (!gotTheLock) {
  app.quit() // 已有实例运行，直接退出
}
```

**效果：**
- ✅ 只允许运行1个应用实例
- ✅ 多次点击图标只会聚焦到已有窗口
- ✅ 任务管理器只显示1个进程

## 📋 测试步骤

### 1️⃣ 重新打包
```bash
npm run build
```

### 2️⃣ 安装测试
1. 安装新生成的 `video-read-along-windows-1.0.0.exe`
2. 打开任务管理器
3. **验证：只显示1个 `video-read-along` 进程**

### 3️⃣ 多实例测试
1. 双击桌面图标启动应用
2. 再次双击桌面图标
3. **验证：不会创建新进程，只是聚焦到已有窗口**

### 4️⃣ CPU占用测试
1. 正常启动应用
2. 选择视频文件播放
3. 打开任务管理器查看
4. **验证：CPU占用应该在5-15%之间，而不是100%+**

### 5️⃣ 功能测试
- [ ] 应用正常启动并显示界面
- [ ] 选择视频文件能正常播放
- [ ] 字幕能正常生成和显示
- [ ] 跟读模式能正常工作

## 🔍 任务管理器检查清单

打开任务管理器（Ctrl+Shift+Esc），切换到"详细信息"标签：

### ❌ 修复前（您的截图）
```
video-read-along  进程1  12.9% CPU
video-read-along  进程2  12.7% CPU
video-read-along  进程3  12.7% CPU
video-read-along  进程4  12.5% CPU
video-read-along  进程5  12.4% CPU
video-read-along  进程6  12.3% CPU
video-read-along  进程7  11.5% CPU
总计: ~87% CPU (卡死！)
```

### ✅ 修复后（应该看到）
```
video-read-along  进程1  8-15% CPU
总计: ~10% CPU (流畅！)
```

## ️ 如果还有问题

### 问题1：仍然有多个进程
**解决方案：**
1. 完全退出应用（右键任务栏图标 → 退出）
2. 打开任务管理器，结束所有 `video-read-along` 进程
3. 重新启动应用
4. 检查是否只有1个进程

### 问题2：CPU仍然很高
**解决方案：**
1. 检查是否选择了超大视频文件（>200MB）
2. 大文件不会自动生成字幕，减少CPU占用
3. 关闭跟读模式测试

### 问题3：应用无法启动
**解决方案：**
1. 等待5秒后重试（单实例锁需要时间释放）
2. 检查任务管理器是否有残留进程
3. 手动结束残留进程后重试

## 📊 您的系统配置

- **内存：** 32GB ✅（非常充足）
- **CPU：** 4核8线程 ✅（性能良好）
- **问题：** 多进程爆炸导致CPU过载 ❌

修复后，您的配置运行这个应用应该**非常流畅**！

##  预期效果

修复后您应该看到：
1. ✅ 任务管理器只有1个进程
2. ✅ CPU占用5-15%
3. ✅ 内存占用100-200MB
4. ✅ 应用响应流畅，不再卡死
5. ✅ 多次点击图标不会创建新进程

---

**重要提示：** 请务必要重新打包并测试，查看任务管理器中的进程数量！这是验证修复是否成功的关键指标。
